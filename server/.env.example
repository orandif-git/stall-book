DATABASE_URL="postgresql://postgres:postgres@localhost:5432/stall_booking?schema=public"
JWT_SECRET="change-me-to-a-long-random-string"
PORT=4000
# Cloudflare Turnstile keys are configured via the admin UI (Integrations page, Super Admin
# only), not here — see WhatsAppSettings.turnstileSiteKey / turnstileSecretKey.
